'use strict';

var Cta = require('../cta');

describe('Cta View', function() {

  beforeEach(function() {
    this.cta = new Cta();
  });

  it('Should run a few assertions', function() {
    expect(this.cta);
  });

});
